---
/**
 * The contact form's answer after a send without JavaScript that failed,
 * English route. Same slug as the German page, so the two pair up by prefix.
 */
import Failed from '../../../components/contact/Failed.astro';
---

<Failed />
