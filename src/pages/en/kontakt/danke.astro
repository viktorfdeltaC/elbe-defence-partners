---
/**
 * The contact form's answer after a send without JavaScript that went through,
 * English route. Same slug as the German page, so the two pair up by prefix.
 */
import Sent from '../../../components/contact/Sent.astro';
---

<Sent />
