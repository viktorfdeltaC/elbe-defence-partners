---
/**
 * The privacy policy, English route — the translation, which says that the
 * German text prevails. Same slug as the German page, so the two pair up by
 * prefix.
 */
import Document from '../../layouts/Document.astro';
import Datenschutz from '../../components/legal/Datenschutz.astro';
---

<Document titleKey="htmlTitlePrivacy" headingKey="privacyTitle">
  <Datenschutz />
</Document>
